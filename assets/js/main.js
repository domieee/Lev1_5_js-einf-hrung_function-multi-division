function math(n1, n2) {
        // Nimmt zwei Summen als Parameter entgegen

    console.log(n1 * n2);
        // Multipliziert beide Summen und gibt das Ergebniss in der Konsole aus

    console.log(n1 / n2);
        // Dividiert beide Summen und gibt das Ergebniss in der Konsole aus

}

math(10,2);